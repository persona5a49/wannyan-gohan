{
  "name": "wannyan-gohan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:products && vite build",
    "preview": "vite preview",
    "check:products": "node scripts/check-product-consistency.js"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
